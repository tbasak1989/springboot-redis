apiVersion: v1
kind: Service
metadata:
  name: springboot-redis-service
spec:
  type: NodePort
  selector:
    app: springboot-redis
  ports:
    - port: 80
      targetPort: 8090
      nodePort: 30080
